# AIHues v8 开发任务清单

> **来源**: ROADMAP.md + plan.md 拆分为可执行的 GitHub Issues
> **格式**: 每个 Issue 包含标题、描述、标签、预估工时、依赖关系

---

## 🏗️ Phase 0: 项目骨架（Week 1 Day 1-2）

### Issue #1: 初始化 Next.js 14 + TypeScript 项目
- **标签**: `infrastructure`, `priority: P0`
- **工时**: 4h
- **描述**:
  ```
  初始化项目骨架：
  - Next.js 14 App Router + TypeScript
  - Tailwind CSS + shadcn/ui
  - ESLint + Prettier 配置
  - .gitignore + README
  - 目录结构按 docs/03-API接口设计.md 的约定建立
  ```
- **验收标准**:
  - `npm run dev` 能启动开发服务器
  - `npm run build` 无报错
  - CI (GitHub Actions) 跑通 Lint

---

### Issue #2: 配置 Prisma + Vercel Postgres
- **标签**: `infrastructure`, `database`, `priority: P0`
- **工时**: 3h
- **描述**:
  ```
  配置数据库层：
  - 安装 Prisma
  - 初始化 schema.prisma（按 docs/02-数据库设计.md）
  - 配置 DATABASE_URL 环境变量
  - 运行首次 migration
  - 配置 seed.ts（插入 57 个工具 + 10 个成就）
  - prisma client 单例封装（lib/prisma.ts）
  ```
- **验收标准**:
  - `npx prisma migrate dev` 成功
  - `npx prisma db seed` 插入 57 条工具记录
  - `npx prisma studio` 能看到数据

---

### Issue #3: 配置 NextAuth.js 认证系统
- **标签**: `infrastructure`, `auth`, `priority: P0`
- **工时**: 4h
- **描述**:
  ```
  配置用户认证：
  - 安装 next-auth v5 (Auth.js)
  - 配置 GitHub OAuth Provider
  - 配置 Google OAuth Provider
  - 配置 Credentials Provider（邮箱+密码）
  - 自定义登录/注册页面（复用 v7 的 login.html 设计）
  - 会话管理（JWT strategy）
  ```
- **验收标准**:
  - GitHub OAuth 登录成功
  - Google OAuth 登录成功
  - 邮箱注册+登录成功
  - 登录后 session 持久化
  - `/api/auth/session` 返回正确用户信息

---

### Issue #4: 配置 next-intl 国际化
- **标签**: `infrastructure`, `i18n`, `priority: P0`
- **工时**: 3h
- **描述**:
  ```
  配置双语支持：
  - 安装 next-intl
  - 创建 messages/zh.json 和 messages/en.json
  - 从 v7 的 i18n.js 提取所有文案
  - 配置 locale routing (/zh, /en)
  - 默认语言：中文
  - 语言切换组件（复用 v7 导航栏设计）
  ```
- **验收标准**:
  - `/zh` 显示中文
  - `/en` 显示英文
  - 语言切换后 URL 和 cookie 更新
  - 所有 UI 文案来自 messages 文件

---

### Issue #5: 提取 v7 设计系统组件
- **标签**: `ui`, `priority: P0`
- **工时**: 6h
- **描述**:
  ```
  从 v7 的 CSS 提取 React 组件：
  - Nav 导航栏组件（含 Logo、链接、主题切换、登录状态）
  - Hero 区域组件（标题 + 搜索框 + 快捷标签）
  - ToolCard 工具卡片（图标 + 名称 + 描述 + 标签）
  - CategoryCard 分类卡片
  - StatsBar 统计栏
  - GameCard 游戏卡片
  - Footer 页脚
  - Button 按钮（渐变、普通、幽灵）
  - Modal 模态框（登录、提示）
  - Toast 通知组件
  - 主题系统（6套皮肤：默认/深色/森林绿/日落橙/极简白/深海蓝）
  ```
- **验收标准**:
  - 所有组件在 Storybook（或独立页面）可预览
  - 主题切换全局生效
  - 深色模式自动跟随系统偏好

---

## 🧩 Phase 1: 核心页面迁移（Week 1 Day 3-5）

### Issue #6: 首页（Hero + 分类导航 + 统计栏）
- **标签**: `page`, `priority: P0`
- **工时**: 8h
- **依赖**: #1, #5
- **描述**:
  ```
  迁移 v7 index.html 为 Next.js 页面：
  - Hero 区：渐变标题 + AI 搜索框 + 快捷标签
  - 分类导航：开发工具 / 实用工具 / AI写作 / 小游戏
  - 统计栏：工具数 / 游戏数 / Credits / 连击天数
  - 热门工具推荐（6个卡片）
  - 小游戏入口（3个）
  - 许愿单 CTA
  - 页脚
  - 智能搜索框（前端实现，支持 / 快捷键）
  ```
- **验收标准**:
  - 与 v7 首页视觉一致
  - `/` 快捷键聚焦搜索框
  - 搜索下拉实时显示工具结果
  - 移动端响应式正常

---

### Issue #7: 发现页（工具列表 + 筛选 + 排序）
- **标签**: `page`, `priority: P0`
- **工时**: 6h
- **依赖**: #2, #6
- **描述**:
  ```
  迁移 v7 discover.html：
  - 工具网格布局（响应式：手机 2列 → 平板 3列 → 桌面 4列）
  - 分类筛选标签
  - 搜索过滤（名称/描述/标签）
  - 排序：默认 / 评分 / 使用次数 / 最新
  - 分页或无限滚动
  - 每个卡片显示：图标、名称、描述、评分、收藏按钮
  ```
- **验收标准**:
  - 57 个工具全部显示
  - 筛选实时过滤
  - 排序切换即时响应
  - 空状态友好提示

---

### Issue #8: 工具详情页（动态路由 [slug]）
- **标签**: `page`, `priority: P0`
- **工时**: 8h
- **依赖**: #2, #5, #7
- **描述**:
  ```
  每个工具独立页面：
  - URL: /tools/[slug]
  - 工具标题 + 描述 + 图标
  - 实际工具功能（从 v7 HTML 提取 JS 逻辑）
  - 评分区域（平均分 + 评分人数 + 评分表单）
  - 评论列表（分页）
  - 收藏按钮
  - 相关工具推荐（同分类）
  - SEO meta 标签（标题/描述/OG图片）
  ```
- **验收标准**:
  - 57 个工具都能访问
  - 评分提交后页面刷新显示新平均分
  - 收藏状态持久化
  - Lighthouse SEO 分数 90+

---

### Issue #9: 排行榜页
- **标签**: `page`, `priority: P1`
- **工时**: 4h
- **依赖**: #2, #3
- **描述**:
  ```
  迁移 v7 ranking.html：
  - 日榜 / 周榜 / 月榜 / 总榜 切换
  - 用户头像 + 名称 + 分数 + 排名
  - 前三名特殊样式（金银铜）
  - 当前用户高亮显示
  - 分页（每页 50 条）
  ```
- **验收标准**:
  - 4 个榜单切换正常
  - 排行榜数据来自 KV/Postgres
  - 自己的排名始终可见

---

### Issue #10: 许愿单页
- **标签**: `page`, `priority: P1`
- **工时**: 4h
- **依赖**: #2, #3
- **描述**:
  ```
  迁移 v7 wishlist.html：
  - 提交新愿望表单
  - 愿望列表（投票数排序）
  - 投票按钮
  - 状态标签（待处理/已批准/开发中/已完成/已拒绝）
  - 筛选按状态
  ```
- **验收标准**:
  - 未登录用户提示登录
  - 投票后实时更新票数
  - 提交新愿望后出现在列表顶部

---

### Issue #11: 个人中心页
- **标签**: `page`, `priority: P1`
- **工时**: 6h
- **依赖**: #2, #3, #5
- **描述**:
  ```
  迁移 v7 profile.html：
  - 用户头像 + 名称 + 注册时间
  - Credits 余额
  - 成就墙（解锁/未解锁）
  - 使用统计（使用工具数 / 评分数 / 连击天数）
  - 收藏的工具列表
  - 最近使用的工具
  - 编辑资料表单
  ```
- **验收标准**:
  - 只能查看自己的个人中心
  - 成就解锁后显示动画
  - 资料编辑实时保存

---

## 🔧 Phase 2: 57 个工具页面迁移（Week 2）

### Issue #12: 开发工具迁移（Batch 1: 15个）
- **标签**: `tools`, `priority: P1`
- **工时**: 16h
- **依赖**: #8
- **工具列表**:
  ```
  JWT Parser, JSON Formatter, Regex Tester, UUID Generator, Timestamp,
  Base64, SHA256, SQL Formatter, URL Encode, Base Converter, Password Gen,
  HTTP Status, HTML Entity, Cron Parser, Code Explain
  ```
- **描述**: 从 v7 HTML 提取每个工具的 JS 逻辑，封装为 React 组件
- **验收标准**:
  - 每个工具功能正常（输入 → 输出）
  - 输入/输出有复制按钮
  - 有使用示例
  - 响应式布局

---

### Issue #13: 开发工具迁移（Batch 2: 15个）
- **标签**: `tools`, `priority: P1`
- **工时**: 16h
- **依赖**: #8
- **工具列表**:
  ```
  Code Review, Shell Gen, Git Commit, IP Lookup, Curl Gen, Diff Pro,
  Unit Convert, Image→Base64, Markdown, Color Convert, CSS Gradient,
  QR Code, Fullwidth, Word Count, Lorem Ipsum
  ```

---

### Issue #14: 实用工具迁移（16个）
- **标签**: `tools`, `priority: P1`
- **工时**: 16h
- **依赖**: #8
- **工具列表**:
  ```
  Calculator, JSON Diff, CSV to JSON, Text Case, All-in-One Encoder,
  Barcode, Image Resize, Color Palette, Timer, Pomodoro, Clipboard,
  Password Strength, Random Gen, Text Stats, Table Gen, SVG to PNG
  ```

---

### Issue #15: AI 写作工具迁移（11个）
- **标签**: `tools`, `priority: P1`
- **工时**: 12h
- **依赖**: #8
- **工具列表**:
  ```
  SEO Title, Blog Outline, X Post, Newsletter, PR Desc, Ad Copy,
  Cold Email, AI Humanizer, Changelog, TL;DR, Readability
  ```
- **注意**: 这些工具需要 AI API（OpenAI/Claude），需要额外配置

---

## 🎮 Phase 3: 游戏 & 功能完善（Week 2-3）

### Issue #16: 3个小游戏迁移
- **标签**: `games`, `priority: P1`
- **工时**: 10h
- **依赖**: #1, #5
- **游戏列表**:
  ```
  1. 日历幸运签 (Fortune) - 每日运势抽签
  2. 老虎机 (Slots) - 三列滚动，消耗 Credits
  3. 投篮挑战 (Hoops) - 计时投篮，排行榜
  ```
- **描述**:
  - 从 v7 games/ 目录提取 JS 逻辑
  - 接入 Credit 系统（老虎机每次 spin -1 Credit）
  - 接入成就系统
  - 接入排行榜（投篮最高分）
- **验收标准**:
  - 3 个游戏都能玩
  - 投篮游戏分数进入排行榜
  - 老虎机 Credit 消耗正常

---

### Issue #17: Credit 经济系统
- **标签**: `feature`, `priority: P1`
- **工时**: 6h
- **依赖**: #2, #3
- **描述**:
  ```
  实现 Credits 的赚取和消耗：
  - 注册赠送 100 Credits
  - 每日登录 +10 Credits
  - 首次使用工具 +5 Credits
  - 给工具评分 +2 Credits
  - 提交许愿 +5 Credits
  - 解锁成就 +N Credits（按成就等级）
  - 消耗：AI写作工具每次 -1 Credit，老虎机每次 -1 Credit
  ```
- **验收标准**:
  - 用户 Credits 余额正确增减
  - 余额不足时提示并禁用消耗功能
  - Credit 变动有 Toast 通知

---

### Issue #18: 成就系统完整实现
- **标签**: `feature`, `priority: P1`
- **工时**: 6h
- **依赖**: #2, #3, #17
- **描述**:
  ```
  实现 10 个成就：
  1. 初次到访 - 第一次访问
  2. 工具探索者 - 使用 5 个不同工具
  3. 重度用户 - 使用 20 个不同工具
  4. 工具大师 - 使用全部 57 个工具
  5. 首次评分 - 给第一个工具评分
  6. 评论家 - 给 10 个工具评分
  7. 三日连击 - 连续 3 天使用
  8. 七日连击 - 连续 7 天使用
  9. 玩家 - 玩 3 个小游戏
  10. 高分选手 - 排行榜前 10
  
  - 解锁时 Toast 通知
  - 成就页面显示进度条
  - 奖励 Credits 自动到账
  ```
- **验收标准**:
  - 每个成就条件检测正确
  - 解锁时显示动画 + 通知
  - 成就页面能看到所有成就状态

---

### Issue #19: 收藏系统
- **标签**: `feature`, `priority: P1`
- **工时**: 4h
- **依赖**: #2, #3, #8
- **描述**:
  ```
  - 每个工具卡片有收藏按钮（心形图标）
  - 收藏后出现在个人中心
  - 收藏数据持久化到数据库
  - 取消收藏即时移除
  ```
- **验收标准**:
  - 收藏状态切换即时响应
  - 个人中心显示收藏列表
  - 换设备登录后收藏仍在

---

## 🚀 Phase 4: 后端功能开发（Week 3）

### Issue #20: 评分系统后端
- **标签**: `backend`, `priority: P0`
- **工时**: 4h
- **依赖**: #2, #3
- **描述**:
  ```
  实现评分 Server Actions：
  - submitRating({ toolId, score, review })
  - getToolRatings(toolId, page)
  - 一个用户只能评一次（upsert）
  - 自动重新计算平均分
  - 刷新页面缓存（revalidatePath）
  ```
- **验收标准**:
  - 评分提交后平均分更新
  - 重复评分覆盖旧评分
  - 评论列表分页正常

---

### Issue #21: 排行榜系统
- **标签**: `backend`, `priority: P1`
- **工时**: 6h
- **依赖**: #2, #17
- **描述**:
  ```
  - 每日定时任务：计算日榜/周榜/月榜
  - 使用 Vercel KV 缓存排行榜
  - 得分算法：使用次数 * 10 + 使用时长 * 0.1 + 评分数 * 5
  - 排行榜页面从 KV 读取（减少数据库压力）
  ```
- **验收标准**:
  - 定时任务每日自动执行
  - 排行榜数据正确排序
  - KV 缓存命中率高

---

### Issue #22: 许愿单后端
- **标签**: `backend`, `priority: P1`
- **工时**: 3h
- **依赖**: #2, #3
- **描述**:
  ```
  - submitWish({ title, description })
  - voteWish(wishId)
  - getWishlist(status)
  - admin 可更新愿望状态
  ```
- **验收标准**:
  - 提交愿望后出现在列表
  - 投票数正确累加
  - 状态更新权限控制

---

## 🧪 Phase 5: 测试 & 部署（Week 3-4）

### Issue #23: 单元测试（核心工具）
- **标签**: `testing`, `priority: P1`
- **工时**: 12h
- **依赖**: #12, #13, #14, #15
- **描述**:
  ```
  用 Vitest 测试高频使用的工具：
  - JSON Formatter（格式化/压缩/校验）
  - Base64（编码/解码）
  - UUID Generator（格式验证）
  - Timestamp（转换正确性）
  - Regex Tester（匹配/替换）
  - JWT Parser（解析正确性）
  
  每个工具至少 3 个测试用例
  ```
- **验收标准**:
  - `npm test` 全部通过
  - 覆盖率 > 70%

---

### Issue #24: E2E 测试（用户流程）
- **标签**: `testing`, `priority: P1`
- **工时**: 8h
- **依赖**: 所有页面 Issue
- **描述**:
  ```
  用 Playwright 测试关键用户流程：
  1. 访问首页 → 搜索工具 → 进入工具页 → 使用工具
  2. 注册 → 登录 → 评分 → 查看个人中心
  3. 收藏工具 → 查看收藏列表 → 取消收藏
  4. 玩游戏 → 查看排行榜
  ```
- **验收标准**:
  - `npm run test:e2e` 全部通过
  - 截图记录关键步骤

---

### Issue #25: SEO 优化
- **标签**: `seo`, `priority: P2`
- **工时**: 4h
- **依赖**: #6, #8
- **描述**:
  ```
  - 每个页面添加 meta title/description/keywords
  - Open Graph 标签（分享时显示卡片）
  - 生成 sitemap.xml（动态）
  - 生成 robots.txt
  - 结构化数据 JSON-LD（Tool 类型）
  - 图片 alt 文本
  ```
- **验收标准**:
  - Lighthouse SEO 分数 95+
  - 社交分享显示正确卡片
  - Google Search Console 验证通过

---

### Issue #26: PWA 支持
- **标签**: `pwa`, `priority: P2`
- **工时**: 3h
- **依赖**: #1
- **描述**:
  ```
  - next-pwa 配置
  - manifest.json（图标/主题色/显示模式）
  - Service Worker（离线缓存核心页面）
  - 安装提示（Add to Home Screen）
  ```
- **验收标准**:
  - Chrome DevTools → Application → Manifest 验证通过
  - Lighthouse PWA 分数 90+
  - 离线状态下能访问首页和已访问的工具页

---

### Issue #27: 性能优化
- **标签**: `performance`, `priority: P2`
- **工时**: 4h
- **依赖**: 所有页面 Issue
- **描述**:
  ```
  - 图片优化（next/image）
  - 字体优化（next/font）
  - 代码分割（工具页面懒加载）
  - 数据库查询优化（添加索引）
  - KV 缓存（排行榜、热门工具）
  ```
- **验收标准**:
  - Lighthouse Performance 分数 90+
  - 首屏加载 < 2s
  - TTI < 3s

---

### Issue #28: Vercel 部署 + 自定义域名
- **标签**: `deployment`, `priority: P0`
- **工时**: 2h
- **依赖**: 所有 Issue
- **描述**:
  ```
  - Vercel 项目配置
  - 环境变量设置
  - 自定义域名绑定
  - SSL 证书自动配置
  - Vercel Analytics 开启
  - Speed Insights 开启
  ```
- **验收标准**:
  - 生产环境能正常访问
  - HTTPS 强制
  - 域名解析正确

---

## 📋 技术债务 & 后续优化

### Issue #29: 旧 v7 工具页面导航统一
- **标签**: `refactor`, `priority: P2`
- **工时**: 4h
- **描述**: v7 中部分旧工具页面的导航栏是旧版，需要统一为新版

### Issue #30: 深色模式全局生效
- **标签**: `ui`, `priority: P2`
- **工时**: 2h
- **描述**: 确保所有 57 个工具页面都支持深色模式

### Issue #31: 管理后台（Admin Dashboard）
- **标签**: `feature`, `priority: P3`
- **工时**: 8h
- **描述**: 管理员查看用户统计、管理许愿单、审核评分

---

## 📊 工时汇总

| 阶段 | Issues | 预估总工时 |
|------|--------|----------|
| Phase 0: 项目骨架 | #1-#5 | 20h |
| Phase 1: 核心页面 | #6-#11 | 36h |
| Phase 2: 工具迁移 | #12-#15 | 60h |
| Phase 3: 游戏&功能 | #16-#19 | 26h |
| Phase 4: 后端 | #20-#22 | 13h |
| Phase 5: 测试&部署 | #23-#28 | 33h |
| **总计** | 28 Issues | **188h ≈ 5 人周** |

---

## 🎯 首批冲刺（Sprint 1: Week 1）

**目标**: 首页 + 发现页 + 登录 + 5个核心工具能跑通

必做 Issues: #1, #2, #3, #4, #5, #6, #7, #8, #12(部分), #20

---

*文档由 AI 助手准备，2026-05-20*
